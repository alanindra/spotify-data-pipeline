Should contain job config

Should have sql model and yaml config for table format.

Each DAG run should check that the table has the same data type with existing table (if available)

All columns must be defined